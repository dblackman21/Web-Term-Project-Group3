function handleLogin(event) {
    event.preventDefault();
    const email = document.getElementById('email').value;
    alert('Connexion process for: ' + email);
    // Connexion process to insert here (backend?)
}
